<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; background: #f6f7fb; margin: 0; padding: 0; color: #1f2540; }
    .container { max-width: 540px; margin: 32px auto; background: #fff; border-radius: 14px; overflow: hidden; box-shadow: 0 8px 32px rgba(15,20,40,.08); }
    .head { padding: 28px 32px 18px; background: linear-gradient(135deg, #16213e, #0f1424); color: #f4f1e8; }
    .eyebrow { font-size: 11px; letter-spacing: 0.18em; color: #e8a04f; text-transform: uppercase; font-weight: 700; }
    .title { font-size: 22px; font-weight: 600; margin-top: 6px; }
    .body { padding: 24px 32px 32px; font-size: 15px; line-height: 1.55; }
    .card { border: 1px solid #e1e4ee; border-radius: 12px; padding: 16px; background: #f9faff; margin: 18px 0; }
    .card .name { font-weight: 600; font-size: 16px; margin-bottom: 4px; }
    .card .meta { color: #6b7390; font-size: 13px; }
    .cta { display: inline-block; background: #e8a04f; color: #0a0e1a; text-decoration: none; padding: 12px 22px; border-radius: 8px; font-weight: 600; margin-top: 8px; }
    .small { font-size: 12px; color: #8a90a8; margin-top: 22px; }
    .small a { color: #1f6fd6; }
  </style>
</head>
<body>
  <div class="container">
    <div class="head">
      <div class="eyebrow">Wanderply</div>
      <div class="title"><%= @inviter.display_name %> wants to share a trip with you</div>
    </div>
    <div class="body">
      <p>Hi,</p>
      <p><strong><%= @inviter.display_name %></strong> (<%= @inviter.email %>) added you to a trip on Wanderply.</p>

      <div class="card">
        <div class="name"><%= @trip.title %></div>
        <% if @trip.destination.present? || (@trip.start_date && @trip.end_date) %>
          <div class="meta">
            <% if @trip.destination.present? %><%= @trip.destination %><% end %>
            <% if @trip.start_date && @trip.end_date %>
              <% if @trip.destination.present? %> · <% end %>
              <%= @trip.start_date.strftime("%b %-d") %> – <%= @trip.end_date.strftime("%b %-d, %Y") %>
            <% end %>
          </div>
        <% end %>
      </div>

      <p>To see the plan, create your account — it takes about 30 seconds. After signing up we'll ask if you want to add this trip to your list.</p>

      <p><a class="cta" href="<%= @url %>">Sign up &amp; view trip →</a></p>

      <p class="small">
        If the button doesn't work, paste this link into your browser:<br>
        <a href="<%= @url %>"><%= @url %></a>
      </p>

      <p class="small">If you weren't expecting this, you can ignore the email — nothing will happen.</p>
    </div>
  </div>
</body>
</html>
