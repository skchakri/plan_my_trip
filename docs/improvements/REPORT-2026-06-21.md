# Trip Coach — improvement backlog · 2026-06-21

_Autonomous daily run (Tier 0, read-only) · Lenses: gap · competitor · ux · trend · **45 raw → 40 ranked** · Claude-subscription · run `wf_3bc39fe0-d3c`_

Score = `impact × (6 − effort) × confidence`. Tags: **[filed]** new myjira ticket today · **[PR#18]** already fixed on the a11y branch (awaiting merge — re-surfaces because the sweep runs on `main`) · **[tracked]** existing open/in_progress ticket · **[backlog]** in this report only.

---

## Carry-over from 2026-06-20

- **PR #18 fixes re-appear here.** The chip swap (#chip), `trivia:seed` (#trivia), public-page `slate-500`, building `slate-600`, concierge/rename labels, pace fieldset, etc. are fixed on `improve/a11y-quickwins-2026-06-20` but **not yet merged**, so this sweep (run on `main`) flags them again. They'll drop off once #18 merges.
- **Still open from yesterday's 8 tickets:** highlight-card aria-name, pace/budget edit form, prompt caching, Turbo morphing, road-trip estimator — unchanged.

## 🆕 Filed to myjira today (8 new, deduped against the 12 existing tickets)

| Ticket | Title | Lens | Score |
|---|---|---|---|
| `202b4018` | **Guard `/trips/:id/plan` while building** (blank no-refresh page) | gap | 20 |
| `45b134aa` | Rescue `DayTripsController#suggestions_results` (5xx in lazy frame) | gap | 15.2 |
| `5f049adf` | **Checklist Edit/Delete `opacity-0` — invisible to touch users** | ux | 15.2 |
| `a130ac22` | Adopt Anthropic **Structured Outputs** for JSON prompts | trend | 15.2 |
| `c9ca2691` | Public share link shows only a title card while building | gap | 15 |
| `9a800462` | Global CSS strips focus outline on all inputs | ux | 14.72 |
| `10a31d6d` | Printable / PDF itinerary export | competitor | 14.25 |
| `8229be5e` | "Navigate day in Google/Apple Maps" handoff | competitor | 14.25 |

_Skipped as duplicates: chip, `trivia:seed`, public `slate-500`, building `slate-600` (→ PR #18); road-trip fuel estimator, prompt caching (→ open tickets)._

---

## 🔝 Top priorities (do next)

1. **`/trips/:id/plan` blank page during build (score 20)** — one-line guard, breaks a linked-everywhere flow. `[filed 202b4018]`
2. **Checklist edit/delete invisible on touch (15.2)** — mobile users literally can't edit their packing list. `[filed 5f049adf]`
3. **Two `*_results` frames render raw 500s (15.2 / 10.8)** — `day_trips#suggestions_results` + `wizard#highlights_results` need rescues. `[filed 45b134aa / backlog]`
4. **Structured Outputs (15.2)** — kills silent JSON build failures at the decoder. `[filed a130ac22]`

---

## 🕳️ Gaps

- **`/trips/:id/plan` blank/no-refresh while building** · `trips_controller.rb:121` · code · 20 · `[filed 202b4018]` — `#show` guards + auto-refreshes; `#plan` doesn't, and the "Final plan" link (`index.html.erb:152`) is unconditional.
- **Drive Co-Pilot `TriviaQuestion` never seeded** · `db/seeds.rb`, `trivia_pool.rb:114` · data · 16 · `[PR#18 / tracked in_progress]` — today's read also flags the `trivia:math`/`trivia:topic` banks as unwired (broader than the PR's `trivia:seed` fix).
- **`day_trips#suggestions_results` no rescue → 5xx in frame** · `day_trips_controller.rb:41` · code · 15.2 · `[filed 45b134aa]`
- **Public share link = title-only card while building** · `public_trips_controller.rb:9`, `show.html.erb:37` · copy · 15 · `[filed c9ca2691]`
- **`invitation_pending` is a dead `Notification::KINDS` value** · `notification.rb:2`, `notification_dispatcher.rb` · code · 12 · `[backlog]` — declared, never dispatched; invitees get email but no in-app notification. Wire it in `trip_shares_controller.rb:88` or drop the enum.
- **`wizard#highlights_results` 500s have no rescue** · `wizard_controller.rb:82` · code · 10.8 · `[backlog]` (skip-button mitigates, but the frame shows garbage).
- **Day-trip empty state has no forward path** · `_results.html.erb:2` · design · 10.8 · `[backlog]` — the "Build a plan" form is inside the `@ideas.any?` branch, so zero ideas = dead end.
- **Blog `show` raises `RoutingError` (→ 500, not 404) for unknown slugs** · `blog_controller.rb:9` · code · 8.5 · `[backlog]` — use `ActiveRecord::RecordNotFound`.
- **`copilot_question.html.erb` dereferences `@question` with no nil guard** · `:12` · code · 8.5 · `[backlog]` — safe today (2-item fallback) but brittle; relates to the seed gap.

## 🎨 UX (design-system + WCAG AA)

- **Legacy ~3.5:1 chip** · `idea_detail:70`, `_more_info:72` · 19.6 · `[PR#18]`
- **`slate-500` readable body text (35+ instances)** · app-wide · 15.2 · `[PR#18 partial / extends tracked #6]` — PR #18 cleared the public page/quizzes/building/concierge; **residual:** `comments/_comment:11`, `quizzes/show:44`, `checklist_items/_checklist_item:24`, `idea_detail`.
- **Checklist Edit/Delete `opacity-0` invisible on touch** · `_checklist_item:28,31` · 15.2 · `[filed 5f049adf]`
- **Global focus-outline suppressed on inputs** · `application.html.erb:43` · 14.72 · `[filed 9a800462]`
- **Form fields with placeholder-only labels** · comments / checklist-add / concierge / add-traveler · 14.25 · `[PR#18 did concierge+rename; backlog: comments/checklist/add-traveler]`
- **`building` spinner + step dots no reduced-motion** · `building.html.erb:53,73` · 14.25 · `[backlog]`
- **Day-trip search input: no `aria-label`, `focus:outline-none`** · `suggestions.html.erb:31` · 13.95 · `[backlog]`
- **Bottom nav < 40px on ≤320px screens** · `application.html.erb:100` · 12.3 · `[backlog]`
- **`animate-*` loaders not `motion-safe:` (21 uses)** · multiple · 11.16 · `[backlog]`
- **Idea modal missing `role=dialog`/`aria-modal`/`aria-labelledby`** · `suggestions.html.erb:117` · 10.8 · `[backlog]`
- **`concierge` thinking dots not `motion-safe`** · `_concierge:62` · 9.3 · `[backlog]`
- **`placeholder:text-slate-600` ~2.2:1** · `places/show:164`, `_highlights_body:340` · 9 · `[backlog]`
- **Packed checklist item `line-through text-slate-500`** · `_checklist_item:21` · 9 · `[backlog]`
- **Wizard eyebrow labels `slate-500` @11px** · 4 wizard views · 8.8 · `[backlog]`
- **Building "Technical details" `slate-600` (~1.9:1)** · `building:43` · 8.7 · `[PR#18]`

## 🏁 Competitors

- **Printable / PDF itinerary export** · new `#export` + print CSS · 14.25 · `[filed 10a31d6d]` (Wanderlog/TripIt; no gems needed)
- **Google/Apple Maps multi-waypoint handoff** · per-day card · 14.25 · `[filed 8229be5e]` (pure frontend)
- **Social URL → itinerary import** · new `UrlImport` + `url_to_places.v1` · 12.8 · `[backlog]` (Mindtrip "Start Anywhere", Expedia Trip Matching; uses existing `claude_cli`)
- **Booking-confirmation email import** · Action Mailbox + `Reservation` · 12.75 · `[backlog]` (TripIt/Wanderlog; impact 5)
- **Group expense tracking + bill splitting** · `Expense`/`ExpenseSplit` · 10.8 · `[backlog]` (Wanderlog free tier; ExchangeRate.host)
- **Route optimization (auto-sort day stops)** · `RouteOptimizer` (TSP, pure Ruby) · 10.8 · `[backlog]`
- **Road-trip fuel estimator** · 10.2 · `[tracked open]`
- **Events discovery scoped to trip dates** · Ticketmaster Discovery (free) · 9 · `[backlog]`
- **Agentic concierge that mutates the itinerary** · `TripAgent` tool-calls · 8 · `[backlog]` — _highest-ceiling AI upgrade; the `trip_agent.rb:12` comment already anticipates it. Uses the Anthropic provider we already have._

## 🚀 Trends

> ⚠️ Several of these optimize/extend the **metered `anthropic` provider** (structured outputs, prompt caching, native web search — web search is **$10/1k searches**, a real metered cost). They pay off only while in-app AI stays on the API vs. all-`claude_cli` subscription. Items below tagged accordingly.

- **Native Structured Outputs** (`output_config.format`) · 15.2 · `[filed a130ac22]` — constrained decoding kills silent JSON build failures. _(metered provider)_
- **Turbo shared-element view transitions** · index→show / suggestions→modal · 13.5 · `[backlog]` — GA, ~10 lines CSS, native-feel morph.
- **Prompt caching (1h TTL)** · `anthropic_provider.rb` · 11.04 · `[tracked open]` _(metered provider)_
- **Anthropic native web-search tool** · replace `claude_cli` for research prompts · 10.56 · `[backlog]` — seconds vs minutes, Kamal-friendly, **but $10/1k searches**. Keep `claude_cli` fallback.
- **`ai:audit_models` rake task** · verify no prompt drifts to a deprecated model (opus-4-1 retires 2026-08-05) · 9.5 · `[backlog]`
- **Rails 8.1 Job Continuations** · `BuildTripJob`/`BackfillTripNarrationsJob` resumable across Kamal deploys · 7.92 · `[backlog]` — fixes "stuck in building" after a mid-flight deploy.
- **WATCH — adaptive thinking on `trip_structure.v1`** · 7.8 · `[backlog]` — pilot in staging first; thinking tokens billed at output rates.

---

## Notes

1. **Today's fresh signal is gaps/bugs, not just a11y.** The standout new defects — `/plan` blank-during-build (#20), checklist buttons invisible on touch, two lazy frames rendering raw 500s, public-share title-only-while-building — are all real broken-flow bugs the yesterday sweep didn't surface. The lenses explored controllers/error-paths more this round.
2. **Recurring theme: build-state coverage.** `#plan`, public-share, and (from the trend lens) Job Continuations all point at the same weak spot — the app doesn't uniformly handle the `building`/`failed` lifecycle outside `#show`. Worth a small focused pass.
3. **Lazy-frame error handling is a pattern bug**, not three separate ones: `day_trips#suggestions_results`, `wizard#highlights_results`, and `trips#brief` all lack a rescue and the `frame-timeout` controller can't catch HTTP 5xx (only network errors). Consider a shared `rescue_from`-style helper or a `turbo:frame-render` 5xx listener in `frame_timeout_controller.js`.
4. **Merging PR #18 will clear ~6 of the recurring a11y items** from the next sweep.

## Carry-over to next run

Open after today: 12 prior tickets + 8 filed today = **20 tracked**. Once PR #18 merges, expect the chip / `trivia:seed` / public-`slate-500` / building-`slate-600` items to stop re-surfacing. The biggest unfiled backlog items are the lazy-frame rescue pattern (#3 above) and the build-state coverage gap (#2 above).
